services:
  - type: web
    name: revive-auctions
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: SHEET_URL
        sync: false
